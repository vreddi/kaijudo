import { v } from "convex/values";

export const race = v.union(
  v.literal("angelCommand"),
  v.literal("apolloniaDragon"),
  v.literal("arcSeraphim"),
  v.literal("armoredDragon"),
  v.literal("armoredWyvern"),
  v.literal("armorloid"),
  v.literal("balloonMushroom"),
  v.literal("beastFolk"),
  v.literal("berserker"),
  v.literal("liquidPeople")
);
