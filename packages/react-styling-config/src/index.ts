/**
 * @kaijudo/react-styling-config
 *
 * This package provides Tailwind CSS v4 theme configuration for @kaijudo component packages.
 *
 * Import CSS files directly:
 * - `@kaijudo/react-styling-config/index.css` - Complete setup (includes Tailwind import)
 * - `@kaijudo/react-styling-config/theme.css` - Theme variables only (no Tailwind import)
 * - `@kaijudo/react-styling-config/base.css` - Base styles only
 *
 * @example
 * ```css
 * // In your app's styles.css
 * @import '@kaijudo/react-styling-config/index.css';
 * ```
 */

// This file exists for TypeScript module resolution
// The actual exports are CSS files via package.json exports

export {};
